# Nixpacks configuration for asian.directory backend
# Used when Railway root directory is set to backend/

[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']

[phases.install]
cmds = ['npm ci --production']

[phases.build]
cmds = []

[start]
cmd = 'node server.js'
